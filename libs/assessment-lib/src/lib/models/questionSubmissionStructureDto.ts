export interface QuestionSubmissionStructureDto {
    questionId: string;
    selectedOptionId: string;
    assessmentId: string;
}

export interface FlagQuestionDto{
  questionId: string;
  assessmentId: string;
}

